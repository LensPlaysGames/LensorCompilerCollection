;; new struct type definition
node :> type {
  kind : integer
  val_int : integer
  val_string : @byte
  a : byte
  next : @node
  b : byte

  foo : type {
    asdf : integer
    hjkl : integer

    bar : type {
      qwer : integer
      tyui : byte
      opas : byte
    }
  }
}

some_node : node

some_node.kind
