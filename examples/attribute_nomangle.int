;; A function may want to retain it's exact name, even in the code
;; generated by the compiler; this may be for use in a program written
;; in another language, like C.
;; Just for fun, to show how `nomangle` is only a contextual keyword,
;; we also call the function nomangle.

nomangle: integer() nomangle {
  69
}

nomangle()
