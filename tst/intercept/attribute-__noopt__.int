;; 42

;; This just tests that this is parsed correctly. This attribute
;; is supposed to disable optimisations, but we can’t test for that
;; at the moment.
foo : int() __noopt__ {
    a :: 21;
    21 + a;
}

foo;
