* (De)Serialise Alignof

Test serialisation and deserialisation of alignof expressions.

#+NAME: source
#+begin_src glint
  import foo_serialise_alignof;

  x 420;
#+end_src

#+NAME: source
#+begin_src glint
  module foo_serialise_alignof;
  export x :: template(e : expr)
    alignof e;
#+end_src

For some odd reason, alignof currently returns bit values instead of byte values (even though alignment is calculated in bytes)...

#+NAME: status
#+begin_example
64
#+end_example

#+NAME: output
#+begin_example
#+end_example
