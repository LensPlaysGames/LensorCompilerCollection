* (De)Serialise NameRefExpr

Test serialisation and deserialisation of name reference expressions.

#+NAME: source
#+begin_src glint
import foo_serialise_nameref;

x 69;
#+end_src

#+NAME: source
#+begin_src glint
module foo_serialise_nameref;

;; `e` is a NameRefExpr, and, as it is the body of an exported named
;; template, will be serialised/deserialised in the BMI. This test is
;; meant to test the DeSer.
export x :: template(e : expr) e;
#+end_src

#+NAME: status
#+begin_example
69
#+end_example

#+NAME: output
#+begin_example
#+end_example
