================
Range-based Loop
:syntax
================

;; for <identifier> in <container-expression> <body-expression>
for x in y, z;

---

(block
 (for
  (block
   (variable_declaration
    (binary_subscript
     (member_access
      (name))
     (member_access
      (name))))
   (variable_declaration
    (member_access
     (name))))
  (binary_notequal
   (name)
   (name))
  (binary_subscript_assign
   (name)
   (integer_literal))
  (block
   (variable_declaration
    (unary_dereference (name)))
   (name))))
