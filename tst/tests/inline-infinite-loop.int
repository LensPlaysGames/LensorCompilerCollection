;; 42

;; Deliberately not `noreturn` so the call to f1() is not deleted.
exit : ext void (code : integer);

;; Mark as used so this doesn’t get deleted.
f1 : integer() used = f2()
f2 : integer() used = f3()
f3 : integer() used = f1()

42
__builtin_inline(f1())